/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // Tailwind CSS v4: a single PostCSS plugin (vendor prefixing is built in).
    '@tailwindcss/postcss': {}
  }
};

export default config;
